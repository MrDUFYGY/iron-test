---
---
  <!-- Botón para abrir el diálogo de subida -->
  <button onclick="showUploadConfirmDialog()" class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">
    Confirmar con Huella
  </button>

  <!-- Modal: Confirmación de subida -->
  <div id="upload-confirm-dialog" class="fixed inset-0 bg-black bg-opacity-50 hidden flex items-center justify-center z-50">
    <div class="bg-white rounded-lg p-6 max-w-md w-full relative z-50">
      <h3 class="text-lg font-semibold mb-4">Confirmar Subida</h3>
      <p class="text-gray-700 mb-6">¿Deseas confirmar la subida del archivo? Se requerirá verificación con huella digital.</p>
      <div class="flex justify-end space-x-4">
        <button id="cancel-upload" class="px-4 py-2 bg-gray-200 text-gray-700 rounded hover:bg-gray-300">Cancelar</button>
        <button id="confirm-upload" class="px-4 py-2 bg-green-600 text-white rounded hover:bg-green-700">Confirmar con Huella</button>
      </div>
    </div>
  </div>

  <!-- Modal: Huella digital -->
  <div id="fingerprint-dialog" class="fixed inset-0 bg-black bg-opacity-50 hidden flex items-center justify-center z-50">
    <div class="bg-white rounded-lg p-6 max-w-md w-full relative z-50">
      <h3 class="text-lg font-semibold mb-4">Validación de Huella Digital</h3>
      <div class="text-center mb-6">
        <div class="w-32 h-32 mx-auto border-2 border-dashed border-gray-300 rounded-full flex items-center justify-center">
          <svg class="w-16 h-16 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
          </svg>
        </div>
        <p class="mt-2 text-gray-600">Coloque su dedo en el lector de huella</p>
      </div>
      <div class="flex justify-end space-x-4">
        <button id="cancel-fingerprint" class="px-4 py-2 bg-gray-200 text-gray-700 rounded hover:bg-gray-300">Cancelar</button>
        <!-- <button id="confirm-fingerprint" class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">Confirmar</button> -->
      </div>
    </div>
  </div>

  <!-- Scripts -->
  <script type="text/javascript">
    // Cierra todos los modales
    function closeAllModals() {
      document.getElementById('upload-confirm-dialog').classList.add('hidden');
      document.getElementById('fingerprint-dialog').classList.add('hidden');
    }

    // Muestra el modal de confirmación de subida
    function showUploadConfirmDialog() {
      closeAllModals();
      document.getElementById('upload-confirm-dialog').classList.remove('hidden');
    }

    // Muestra el modal de huella digital
    function showFingerprintDialog() {
      closeAllModals();
      document.getElementById('fingerprint-dialog').classList.remove('hidden');
    }

    // Listeners al cargar la página
    document.addEventListener('DOMContentLoaded', function () {
      document.getElementById('cancel-upload').addEventListener('click', closeAllModals);
      document.getElementById('confirm-upload').addEventListener('click', function () {
        showFingerprintDialog();
      });

      document.getElementById('cancel-fingerprint').addEventListener('click', closeAllModals);
      document.getElementById('confirm-fingerprint').addEventListener('click', function () {
        closeAllModals();
        alert('Huella confirmada. Subida completada.');
        window.location.href = '/';   
      });
    });
  </script>
