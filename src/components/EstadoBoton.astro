---
export interface Props {
  url: string;
  alerta?: boolean;
}
const { url, alerta = false } = Astro.props;
---
<div class="flex items-center gap-2 min-w-[220px] justify-end">
  <a href={url} class="text-blue-600 font-medium px-3 py-1 rounded-lg border border-blue-600 hover:bg-blue-50 flex items-center gap-1">
    Verificar
  </a>
  {alerta && (
    <span class="bg-red-100 text-red-700 font-medium px-3 py-1 rounded-lg border border-red-400 ml-2">
      ¡Alerta!
    </span>
  )}
</div> 