---
// Menu.astro
interface Props {
  items: Array<{
    title: string;
    description: string;
    icon: string;
    link: string;
    validadoGerente?: boolean;
    validadoCorporativo?: boolean;
    sugerencia?: boolean;
  }>;
}

const { items } = Astro.props;
---

<div class="menu-container">
  <div class="menu-grid">
    {items.map((item) => (
      <a href={item.link} class={`menu-card ${item.sugerencia ? 'sugerencia' : ''}`}>
        <div class="card-top">
          <div class="card-icon">
            <span class="material-icons">{item.icon}</span>
          </div>
          <div class="card-content">
            <h3>{item.title}</h3>
            <p>{item.description}</p>
          </div>
        </div>
        <div class="validaciones">
          <span class={`badge badge-gerente ${item.validadoGerente ? 'lleno' : ''}`}>Gerente</span>
          <span class={`badge badge-corporativo ${item.validadoCorporativo ? 'lleno' : ''}`}>Corporativo</span>
          {item.sugerencia && <span class="badge badge-sugerencia">Sugerencia</span>}
        </div>
      </a>
    ))}
  </div>
</div>

<style>
  .menu-container {
    padding: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .menu-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
  }

  .menu-card {
    border: 2px solid #14532d;
    background: #1e293b;
    border-radius: 12px;
    padding: 1.5rem;
    text-decoration: none;
    color: #f8fafc;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    text-align: center;
    position: relative;
    height: 100%;
  }

  .menu-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
  }

  .menu-card.sugerencia {
    border-color: #facc15;
    background-color: #fef9c3;
    color: #78350f;
  }

  .card-top {
    flex-grow: 1;
  }

  .card-icon {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: #4ade80;
  }

  .card-content h3 {
    margin: 0 0 0.5rem 0;
    font-size: 1.25rem;
    color: inherit;
  }

  .card-content p {
    margin: 0;
    font-size: 0.875rem;
    color: inherit;
  }

  .validaciones {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
    justify-content: center;
    margin-top: 1.5rem;
  }

  .badge {
    padding: 0.25rem 0.6rem;
    border-radius: 999px;
    font-size: 0.75rem;
    font-weight: bold;
    border: 2px solid;
    background-color: transparent;
  }

  .badge-gerente {
    border-color: #22c55e;
    color: #22c55e;
  }

  .badge-corporativo {
    border-color: #eab308;
    color: #eab308;
  }

  .badge-sugerencia {
    border-color: #f59e0b;
    color: #f59e0b;
  }

  .badge.lleno.badge-gerente {
    background-color: #22c55e;
    color: #fff;
  }

  .badge.lleno.badge-corporativo {
    background-color: #eab308;
    color: #333;
  }

</style>
