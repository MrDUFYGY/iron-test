---
const modalId = 'huellaLogin-modal';
---

<dialog id={modalId} class="rounded-xl shadow-lg p-6 backdrop:bg-black/30 w-full max-w-md">
  <h2 class="text-lg font-bold text-gray-800 mb-4">Ingresa la huella</h2>
  <p class="text-gray-600 mb-6">Por favor, coloca tu dedo en el lector de huellas...</p>
  <form method="dialog" class="flex justify-end gap-2">
    <button
      type="submit"
      class="bg-gray-300 hover:bg-gray-400 text-gray-800 px-4 py-2 rounded-lg transition"
    >Cancelar</button>
    <button
      onclick="window.location.href = 'muestrario_valija_0menuEstacion'"
      class="bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded-lg transition"
    >Continuar</button>
  </form>
</dialog>

<!--<script is:inline>
  document.addEventListener("DOMContentLoaded", () => {
    const modal = document.getElementById("{modalId}");
    if (modal?.showModal === undefined) {
      alert("Tu navegador no soporta <dialog>.");
    }
  });
</script>  -->
