---
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <!-- Fondo con imagen -->
  <div 
    class="fixed inset-0 bg-cover bg-center z-40" 
    style="background-image: url('../../public/assets/hidrosina-3.jpg');"
  ></div>

  <!-- Contenido del modal centrado -->
  <div class="fixed inset-0 flex items-center justify-center z-50">
    <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-md text-center">
      <h1 class="text-2xl font-bold mb-4">
        <br /> ¿Estas seguro de confirmar las subida de documentos?
      </h1>

        <button
        id="btnIrProceso"
        onclick="window.location.href='muestrario_valija_0menuEstacion'"
        class="bg-gray-500 text-white px-6 py-2 rounded hover:bg-gray-600 transition mb-4"
      >
        Volver
      </button>

      <button
        id="btnIrProceso"
        onclick="window.location.href='form_ConfirmacionFinal'"
        class="bg-blue-500 text-white px-6 py-2 rounded hover:bg-blue-600 transition mb-4"
      >
        Confirmar
      </button>
    </div>
  </div>
</Layout>

